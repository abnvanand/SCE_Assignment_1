# TODO: Ask should i write only the regex in this file
# or the entire grep command
# what's the input file for this if I write the grep command?
#grep -E "^[[:digit:]]{3}\.[[:digit:]]{3}\.[[:digit:]]{3}\.[[:digit:]]{3}$"
